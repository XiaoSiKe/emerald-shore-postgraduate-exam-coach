# V0.1.0 评测报告

评测日期：2026-09-09。

## 自动化结果

- 本地：24 项测试中 23 项通过；仅因主环境未安装可选 `pypdf` 跳过 1 项。该 PDF 文本分支在隔离环境安装 `pypdf` 后单独通过。
- GitHub Actions：Windows 3.9/3.12、Ubuntu 3.9/3.12、macOS 3.12 全部通过；每个环境均安装 `pypdf`，24 项测试全部执行。
- 发布包：两次构建 SHA-256 一致，ZIP 只有 `qingan-kaoyan-coach/` 一个根目录。
- 安装冒烟：解压后 `--version`、`init`、`subject add`、`plan`、`today` 全部通过。
- 静态行为契约：14/14，通过率 100%；来源、隐私、安全和不承诺关键项 8/8。
- 上游锁定：9 个来源均能按仓库或目标文件 commit 复核，无未解释漂移。

主分支证据：[CI run 34323910509](https://github.com/XiaoSiKe/qingan-kaoyan-coach/actions/runs/34323910509)。

## 修复记录

首轮云端测试发现 Windows 重定向输出无法编码中文。CLI 现按输出流能力选择可读 Unicode 或 ASCII 转义 JSON；随后 Windows 3.9/3.12 均通过。初始化也改为整目录原子提交，复习合并任务增加了队列推进回归测试。

## 未自动化范围

固定场景验证的是 Skill 明文行为契约，不冒充真实模型对话评测。V0.1.0 尚未绑定某个模型供应商的可重复 live-eval；不同模型是否始终遵循“一次一题、来源诚实和情绪边界”仍需在后续版本持续采样。确定性 CLI、跨平台行为和发布包不依赖模型。
